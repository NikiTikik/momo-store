upstream backend {
    server backend:8081;
    }

server {
    listen       80;
    location / {
        root   /app/static;
        index  index.html index.htm;
    }

location /products
    {
     proxy_pass    http://backend;
    }
 }
